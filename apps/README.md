# Apps

This directory contains user-facing applications for `Lenker MVP v0.1`.

Included applications:

- `panel-web` — React + TypeScript provider panel
- `client-app` — Flutter app for Android, Windows, and macOS

Not included:

- marketplace-only application
- iOS application target
- white-label provider-specific app builds
